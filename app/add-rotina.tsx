import { SafeAreaView } from "react-native";
import AddRotinaModal from "@/views/AddRotinaModal";

export default function AddRotina () {
    return(
        <SafeAreaView>
            <AddRotinaModal />
        </SafeAreaView>
    );
};